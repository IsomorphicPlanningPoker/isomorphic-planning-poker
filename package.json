{
  "name": "isomorphic-planning-poker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test-debug": "NODE_ENV=test jasmine-node-debug JASMINE_CONFIG_PATH=jasmine.json",
    "test": "NODE_ENV=test jasmine JASMINE_CONFIG_PATH=jasmine.json",
    "preinstall": "rm -rf dist && rm -rf typings",
    "postinstall": "typings install",
    "prebuild": "npm install",
    "build": "tsc -p shared"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "deep-freeze": "0.0.1",
    "immutable": "^3.8.1",
    "lodash": "^4.13.1"
  },
  "devDependencies": {
    "jasmine": "^2.4.1"
  }
}
